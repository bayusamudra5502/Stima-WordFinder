#pragma once

#include <string>
#include <vector>

#include "../lib/types.hpp"

using namespace std;

namespace Console {
void printTable(vector<string> charTable, vector<vector<Color>> colorTable);
}